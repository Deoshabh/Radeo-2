# ===== NextAuth Core Configuration =====
# Base URL of your website - must match the URL the site is accessed from
# Do not include trailing slash
NEXTAUTH_URL=http://localhost:3000

# Secret used for signing cookies and tokens
# Use a strong random string (32+ characters)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=YOUR_SECURE_RANDOM_SECRET_HERE

# ===== API Configuration =====
# URL of your backend API
# Must be accessible from both server and client environments
NEXT_PUBLIC_API_URL=http://localhost:5000

# ===== Optional Social Providers =====
# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Facebook OAuth
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# ===== Optional Email Provider =====
# Only needed if you want to enable email authentication
EMAIL_SERVER_HOST=
EMAIL_SERVER_PORT=
EMAIL_SERVER_USER=
EMAIL_SERVER_PASSWORD=
EMAIL_FROM=

# ===== Authentication Debug Options =====
# Enable detailed auth logs by setting to true
NEXTAUTH_DEBUG=false

# ===== Cookie Configuration =====
# Only set these if you need to override defaults
# NEXTAUTH_COOKIE_DOMAIN=localhost
# NEXTAUTH_COOKIE_SECURE=false
